import { EchartsTsChartDirective } from './echarts-ts-chart.directive';

describe('EchartsTsChartDirective', () => {
  it('should create an instance', () => {
    const directive = new EchartsTsChartDirective();
    expect(directive).toBeTruthy();
  });
});
